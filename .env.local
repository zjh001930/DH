# =============================================================================
# AI 助手工具 - 本地环境配置文件
# 用于在本地直接运行脚本时使用
# =============================================================================

# -----------------------------------------------------------------------------
# 数据库配置 (PostgreSQL) - 本地访问
# -----------------------------------------------------------------------------
POSTGRES_DB=ai_assistant
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# 完整的数据库连接 URL - 本地访问
DATABASE_URL=postgresql://postgres:password@localhost:5432/ai_assistant

# -----------------------------------------------------------------------------
# LLM 和 Embedding 模型配置 - 本地访问
# -----------------------------------------------------------------------------
# Ollama 服务地址 - 本地访问
OLLAMA_API_URL=http://localhost:11434

# 使用的模型名称
LLM_MODEL_NAME=qwen2.5:3b-instruct
EMBEDDING_MODEL_NAME=bge-m3

# -----------------------------------------------------------------------------
# 向量数据库配置 (Weaviate) - 本地访问
# -----------------------------------------------------------------------------
WEAVIATE_HOST=localhost:8080
WEAVIATE_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# 服务端口配置
# -----------------------------------------------------------------------------
# 后端 API 端口
BACKEND_PORT=8000

# 前端服务端口
FRONTEND_PORT=3000

# PostgreSQL 端口
POSTGRES_PORT=5432

# Weaviate 端口
WEAVIATE_PORT=8080

# Ollama 端口
OLLAMA_PORT=11434

# -----------------------------------------------------------------------------
# 其他配置
# -----------------------------------------------------------------------------
INTENT_CONFIDENCE_THRESHOLD=0.75
IMAGE_STORAGE_PATH=/app/data/images
FLASK_ENV=development
FLASK_DEBUG=1
LOG_LEVEL=INFO